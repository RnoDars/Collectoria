# Server Configuration
SERVER_PORT=8080

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=collectoria
DB_PASSWORD=collectoria
DB_NAME=collection_management
DB_SSLMODE=disable

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
CORS_MAX_AGE=300

# JWT Configuration
# Generate a secure secret with: openssl rand -base64 48
JWT_SECRET=your-super-secret-key-at-least-32-characters-long-please-change-me-in-production
JWT_EXPIRATION_HOURS=24
JWT_ISSUER=collectoria-api
